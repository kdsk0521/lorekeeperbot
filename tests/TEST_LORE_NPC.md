# 로어 및 NPC 처리 테스트 가이드

## 문제 설명
로어를 저장할 때 NPC 정보가 분리되어 저장되지만, AI가 응답을 생성할 때 NPC 정보가 제대로 반영되지 않는 문제가 있었습니다.

## 해결 방법
`get_lore_with_npcs()` 함수를 추가하여 AI에게 전달할 때 로어와 NPC 정보를 합쳐서 전달하도록 수정했습니다.

## 테스트 시나리오

### 1. 로어 저장 및 NPC 추출 테스트

**입력:**
```
!로어 [파일 업로드 또는 텍스트]

예시 로어:
===
이 세계는 마법과 과학이 공존하는 판타지 세계입니다.
도시 네오-아크론에서는 마법사와 기술자들이 협력하여 살아갑니다.

리엘: 엘프 궁수로, 과묵하지만 활솜씨가 뛰어납니다. 도시 외곽에서 경비를 서고 있습니다.

가렌: 용감한 전사로, 정의감이 강합니다. 모험가 길드의 리더입니다.
===
```

**예상 결과:**
1. AI가 로어에서 NPC 2명(리엘, 가렌)을 추출
2. 정리된 로어(NPC 제외)와 원본 로어(NPC 포함)가 별도로 저장됨
3. 응답 메시지:
   ```
   ✅ [분석 완료]
   장르: ['high_fantasy']
   NPC 추출: 2명
   ```

### 2. 로어 조회 테스트

**입력:**
```
!로어
```

**예상 결과:**
```
📜 로어 정보

📚 원본 (NPC 포함): XXX자
📖 정리된 로어 (NPC 제외): YYY자
👥 추출된 NPC: 2명

🎭 장르: high_fantasy
🎨 톤: [분석된 톤]

👥 NPC 목록 (미리보기):
• 리엘: 엘프 궁수로, 과묵하지만 활솜씨가 뛰어남...
• 가렌: 용감한 전사로, 정의감이 강함...

📄 정리된 로어 미리보기:
```
이 세계는 마법과 과학이 공존하는 판타지 세계입니다...
```
```

### 3. AI 응답에 NPC 정보 반영 테스트

**시나리오:**
1. 위의 로어와 NPC가 저장된 상태에서 세션 시작
2. 플레이어가 행동: "리엘을 찾아간다"

**예상 AI 응답:**
AI는 리엘의 정보(엘프 궁수, 과묵, 활솜씨 뛰어남, 도시 외곽 경비)를 알고 있어야 하며, 이에 맞는 응답을 생성해야 합니다.

예시:
```
도시 외곽의 경비 초소로 향하자, 나무 그늘 아래 서 있는 엘프의 모습이 보인다. 
리엘은 활을 손질하고 있다가 당신의 발소리를 듣고 고개를 들었다.
그녀의 녹색 눈이 당신을 잠시 살핀다. 말없이 고개를 끄덕이는 것이 인사인 듯하다.
```

### 4. NPC 목록 조회 테스트

**입력:**
```
!npc
```

**예상 결과:**
```
👥 NPC 목록
• 리엘 (Active): 엘프 궁수로, 과묵하지만 활솜씨가 뛰어남...
• 가렌 (Active): 용감한 전사로, 정의감이 강함...
```

### 5. AI 분석 명령어 테스트

**입력:**
```
!분석 리엘의 성격은 어떤가?
```

**예상 결과:**
AI가 로어의 NPC 정보를 참고하여 리엘이 "과묵하다"는 정보를 기반으로 분석 결과를 제공해야 합니다.

## 기술적 구현 세부사항

### 데이터 흐름

1. **로어 저장 시:**
   ```
   유저 입력 → extract_npcs_with_segments() → 
   - 원본 로어 저장 (save_lore_original)
   - NPC 추출 및 저장 (npc_memory.add_npc)
   - 정리된 로어 저장 (append_lore)
   ```

2. **AI 응답 생성 시:**
   ```
   get_lore_with_npcs() →
   - get_lore() [정리된 로어 로드]
   - get_npcs() [NPC 목록 로드]
   - 로어 + NPC 섹션 결합
   → AI에게 전달
   ```

### 주요 함수

- `domain_manager.get_lore()`: 정리된 로어만 반환 (NPC 제외)
- `domain_manager.get_lore_with_npcs()`: 정리된 로어 + NPC 섹션 반환
- `domain_manager.get_lore_original()`: 원본 로어 반환 (NPC 포함)
- `domain_manager.get_npcs()`: NPC 딕셔너리 반환

## 확인 포인트

### ✅ 성공 기준
- [ ] 로어 저장 시 NPC가 정확히 추출되어 별도 저장됨
- [ ] AI 응답에 NPC 정보가 정확히 반영됨 (이름, 성격, 역할 등)
- [ ] `!로어` 명령어로 NPC 개수 및 목록 미리보기 확인 가능
- [ ] `!npc` 명령어로 전체 NPC 목록 확인 가능
- [ ] AI 분석 명령어(`!분석`, `!일관성`, `!세계규칙`)에서 NPC 정보 활용

### ❌ 실패 사례
- NPC가 로어에 있지만 AI가 해당 NPC를 모르는 것처럼 응답
- NPC 목록이 비어있거나 불완전함
- AI가 NPC의 특성과 맞지 않는 행동/대사를 생성

## 디버깅 팁

1. **NPC가 추출되지 않는 경우:**
   - 로어의 NPC 설명 형식 확인 (AI가 인식 가능한 형태인지)
   - `extract_npcs_with_segments()` 함수의 프롬프트 검토

2. **AI가 NPC 정보를 반영하지 않는 경우:**
   - `get_lore_with_npcs()`가 올바르게 호출되는지 확인
   - 생성된 NPC 섹션이 올바른 형식인지 확인
   - AI 프롬프트에 로어가 제대로 포함되는지 확인

3. **로그 확인:**
   ```python
   logging.info(f"[Lore] Length: {len(lore_txt)}")
   logging.info(f"[NPCs] Count: {len(npcs)}")
   ```
